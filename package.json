{
  "name": "viperpad-editor-assets",
  "private": true,
  "scripts": {
    "build": "esbuild web/editor.js --bundle --format=iife --outfile=src/editor.bundle.js",
    "check:bundle": "npm run build && git diff --exit-code -- src/editor.bundle.js",
    "test": "npm run check:bundle"
  },
  "dependencies": {
    "@codemirror/commands": "^6.8.1",
    "@codemirror/language": "^6.11.3",
    "@codemirror/language-data": "^6.5.2",
    "@codemirror/state": "^6.5.2",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.38.1",
    "codemirror": "^6.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
